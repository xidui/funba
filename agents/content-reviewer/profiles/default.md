# Default Review Profile

Use this profile when an issue does not map cleanly to a known content pipeline.

Apply the full generic checklist in `agents/content-reviewer/AGENTS.md`.

Before approving, identify the intended platform mix and content shape from the variants and destinations. If the post behaves like a known pipeline, follow the closest matching profile as an additional checklist.
